apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: wp-web-hpa
  namespace: wp-mysql-stack
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: wp-web
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
